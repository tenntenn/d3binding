define(
        'd3binding/main',
        [
                'd3binding/binding'
        ],
        function(binding) {
                return {
                        binding: binding
                };
        }
);
